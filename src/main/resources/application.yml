spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pokemon?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: mysql
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
server:
  servlet:
    context-path: /poke-api
  port: 8080